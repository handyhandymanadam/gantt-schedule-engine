/**
 * gantt-schedule-ui
 *
 * A reference Gantt renderer for `gantt-schedule-engine`. Vanilla DOM, no framework, no
 * dependencies, themed entirely through CSS custom properties.
 */

export { createGantt } from './gantt.js'
export type { GanttInstance, GanttOptions, Zoom } from './gantt.js'
